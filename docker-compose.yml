version: "3.5"

services:
    nginx:
        build: ./nginx
        image: nginx
        ports:
            - "443:443"
    mariadb:
        build: ./mariadb
        image: mariadb
        ports:
            - "3306:3306"
    wordpress:
        build: ./wordpress
        image: wordpress
        ports:
            - "9000:9000"
